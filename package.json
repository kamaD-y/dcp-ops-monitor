{
  "name": "dcp-ops-monitor",
  "version": "0.1.0",
  "bin": {
    "dcp-ops-monitor": "bin/dcp-ops-monitor.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test:cdk": "jest --testPathPatterns ./test/cdk/*.test.ts",
    "test:web-scraping": "cd lambda/web-scraping && ENV=test uv run pytest --cov -v --tb=short --disable-warnings --maxfail=1",
    "test:error-notification": "cd lambda/error-notification && ENV=test uv run pytest --cov -v --tb=short --disable-warnings --maxfail=1",
    "test:summary-notification": "cd lambda/summary-notification && ENV=test uv run pytest --cov -v --tb=short --disable-warnings --maxfail=1",
    "cdk": "cdk",
    "lint": "biome lint --write . && uvx ruff check",
    "lint:ci": "biome lint . && uvx ruff check --no-fix",
    "type-check": "cd lambda/web-scraping && uvx ty check src/ --ignore unresolved-import --ignore unresolved-attribute",
    "type-check:all": "npm run type-check",
    "format": "biome format --write . && uvx ruff format",
    "format:ci": "biome format . && uvx ruff format --check"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "@types/jest": "^30.0.0",
    "@types/node": "25.0.3",
    "dotenv": "^17.2.3",
    "globals": "^16.5.0",
    "jest": "^30.2.0",
    "lefthook": "^2.0.13",
    "lint-staged": "^16.2.7",
    "ts-jest": "^29.4.6",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.51.0"
  },
  "dependencies": {
    "@aws-cdk/aws-lambda-python-alpha": "^2.233.0-alpha.0",
    "aws-cdk-lib": "^2.233.0",
    "constructs": "^10.4.4"
  }
}
